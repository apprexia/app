import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
    selector: 'app-rgpd',
    imports: [RouterLink],
    templateUrl: './rgpd.html',
    styleUrl: './rgpd.scss',
})
export class Rgpd {}
